let initialState = {
  name: [
    { id: 1, name: "Vladimir" },
    { id: 2, name: "Lizan" },
    { id: 3, name: "Petrica" },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
